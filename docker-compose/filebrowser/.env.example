# Copy to .env and fill private values before use. Blank required values prevent startup.
FB_NOAUTH=false
FILEBROWSER2_IMAGE=filebrowser/filebrowser:latest
FILEBROWSER3_IMAGE=filebrowser/filebrowser:latest
MEDIA2_DIR=/mnt/media2/Media
MEDIA3_DIR=/mnt/media3/Media
