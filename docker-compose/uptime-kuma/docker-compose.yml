services:
  uptime-kuma:
    image: "${UPTIME_KUMA_IMAGE:?Set UPTIME_KUMA_IMAGE to the intended image tag}"
    container_name: uptime-kuma
    ports:
    - 3001:3001
    volumes:
    - "./uptime-kuma-data:/app/data"
    restart: always
