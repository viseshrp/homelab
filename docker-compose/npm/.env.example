# Copy to .env and fill private values before use. Blank required values prevent startup.
X_FRAME_OPTIONS=sameorigin
APP_IMAGE=jc21/nginx-proxy-manager:latest
DOCKER_LOG_MAX_SIZE=10m
DOCKER_LOG_MAX_FILES=3
